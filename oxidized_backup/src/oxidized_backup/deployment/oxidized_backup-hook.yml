hooks:
  checkmk_oxidized_backup_state:
    type: exec
    events:
      - node_success
      - node_fail
      - post_store
    cmd: >-
      /usr/bin/oxidized_backup_hook
      --record-hook
      --config /etc/check_mk/oxidized_backup.json
    timeout: 10
    async: false
